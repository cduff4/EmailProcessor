*** UPDATE THIS README FILE WITH NEWLY DISCOVERED CAVEATS AND OTHER IMPORTANT INFORMATION ***

Be sure to update Test project for future changes and run all tests before proceeding with pushing changes and installing app to server.

Update AssemblyFileVersion in cduff.EmailProcessor >> Properties >> AssemblyInfo.cs to the new major.minor.hotfix version number to set version for install.
	Other project version numbers should be updated as well but the one referenced above will determine the official version of the installed program.